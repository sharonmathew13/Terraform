Provider Dependency file just store the version of the providers and checksum values.
so upgrade of the version is determined, inorder to avoid error when the Resource gets updated with new versions  
cmd : terraform init --upgrade
